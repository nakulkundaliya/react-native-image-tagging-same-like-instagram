/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,

} from 'react-native';

import PhotoTagging from './src/app';

AppRegistry.registerComponent('PhotoTagging', () => PhotoTagging);
